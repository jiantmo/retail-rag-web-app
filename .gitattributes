# Git LFS Configuration
# Track large files with Git LFS

# JSONL files (search results)
*.jsonl filter=lfs diff=lfs merge=lfs -text

# Large JSON files (over 50MB)
*product_test_results*.json filter=lfs diff=lfs merge=lfs -text

# Other large data files
*.csv filter=lfs diff=lfs merge=lfs -text

# Binary files that might be large
*.zip filter=lfs diff=lfs merge=lfs -text
*.tar filter=lfs diff=lfs merge=lfs -text
*.gz filter=lfs diff=lfs merge=lfs -text
*.bz2 filter=lfs diff=lfs merge=lfs -text
*.7z filter=lfs diff=lfs merge=lfs -text

# Database files
*.db filter=lfs diff=lfs merge=lfs -text
*.sqlite filter=lfs diff=lfs merge=lfs -text

# Large text files that might contain data
*_results.json filter=lfs diff=lfs merge=lfs -text
